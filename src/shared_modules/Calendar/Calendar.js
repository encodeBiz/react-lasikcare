import React from 'react'
import "./Calendar.scss"

const Calendar = ({children}) => {
    return (
        <div>
            Calendar
            {children}
        </div>
    )
}

export default Calendar
